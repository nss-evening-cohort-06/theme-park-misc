"use strict";

const data = require('./data');
const time = require("./time");
const dom = require('./dom');
const events = require('./events'); 

data.retrieveKeys(); 
events.testFunction();
events.init();
